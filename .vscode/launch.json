{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch TypeScript",
      "preLaunchTask": "TSC Compile",
      "cwd": "${workspaceFolder}",
      "program": "${file}",
      "console": "internalConsole"
    }
  ]
}